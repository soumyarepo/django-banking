apiVersion: v1
kind: Secret

metadata:
  name: {{ include "banking-app.fullname" . }}-secret

  labels:
    {{- include "banking-app.labels" . | nindent 4 }}

type: Opaque

stringData:
  DJANGO_SECRET_KEY: {{ .Values.secret.djangoSecretKey | quote }}